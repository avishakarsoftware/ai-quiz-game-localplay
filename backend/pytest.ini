[pytest]
# Hangs become failures with a dumped stack instead of a stuck run.
# test_e2e.py historically hung the full suite (sync TestClient WS receives);
# 120s is generous for any single test here.
timeout = 120
timeout_method = thread
